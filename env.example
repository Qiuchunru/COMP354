# AI Configuration
OPENAI_API_KEY=your-key-here
OPENAI_MODEL=gpt-4o-mini

# Sponsors data (renamed from .db to match the csv format proposed in the diagram outputs)
SPONSOR_CSV_PATH=data/sponsors.csv

# Pipeline settings
MIN_OVERALL_SCORE=6.0
MAX_CRAWL_PAGES=50
MAX_EMAILS_PER_SITE=5

# Discovery
MLH_EVENTS_URL=https://www.mlh.io/seasons/2026/events

# Would be nice to swap hardcoded values for variables in the email generator scripts, like:
# SENDER_NAME, SENDER_TITLE, EVENT_NAME, EVENT_DATE, EVENT_VENUE and handle by config.py in Setting class once completed



